# AI provenance: 2026-09-24, MCP server (release 0.3.0)

Recorded under the project's [AI use disclosure](../../AI-USE.md), following the
[NLnet policy on generative AI](https://nlnet.nl/foundation/policies/generativeAI/).

## Session

| | |
|---|---|
| Date | 2026-09-24 (Europe/Zurich) |
| Model | Anthropic Claude Opus 5.5 (`claude-opus-5-5`), as a Claude Code sub-agent |
| Interface | Claude Code CLI, working directly in the repository |
| Branch | `feature/mcp-server`, from `develop` |
| Operator | Julian Laycock (maintainer) |

## Instructions given

The maintainer approved building "MCP server for eu-reg-feed" as a portfolio
piece, from a list of options the assistant proposed. The assistant wrote the
detailed brief for the sub-agent: an MCP server over the published feed, with
tools for search, lookup, upcoming deadlines and sources; zod input validation;
tests; a README with a quick start and an architecture diagram; no push and no
publish without the maintainer's approval.

The maintainer then instructed: "we'll push the MCP server", and approved a
0.3.0 release to main.

## What the assistant produced

- `mcp-server/`: the server source (`src/`), tests with a fixture feed
  (`tests/`), package config and README.
- Root `README.md`: a short "MCP Server" section.
- `eslint.config.js`, `jest.config.mjs`: ignore `mcp-server/`.

## Review status

Honest status at release: the code was generated by the assistant and verified
by automated checks, which the assistant ran:
- `npx tsc --noEmit`;
- `npx vitest run`, 33 of 33 passing;
- the root lint and build;
- `npm test`, 96 of 96 passing;
- a manual stdio smoke test against `feed/latest.json`.

The maintainer has **not** reviewed it line by line. No time for this work is
claimed under the grant until that review is done.
